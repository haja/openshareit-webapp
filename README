# Dependencies:
#
# python3.3
# pip
# virtualenv
#

# setup virtualenv
virtualenv-3.3 env
source env/bin/activate

# install requirements (TODO do this in virtualenv setup script)
pip install -r requirements.txt

# run local server
python foodsharing_webui/manage.py runserver
